# バーチャル家具配置ツール 取扱説明書

本システムは、空室の写真にAI（Gemini）を用いて自動的に家具を配置し、バーチャルステージングを行うためのツールです。

---

## 👨‍💼 管理者向けマニュアル

管理者は `admin` 権限でログインし、以下のシステム全体の設定やクライアントアカウントの管理を行います。

### 1. 初期ログイン
- **ID:** `admin`
- **パスワード:** デフォルトは `admin123` （※環境変数 `ADMIN_PASSWORD` で変更可能）
- ログイン後、「システム管理画面へ」ボタンを押して管理ダッシュボードに移動します。

### 2. APIキーの設定（必須）
システムの画像生成には Google Gemini API を使用します。
1. **[APIキー設定]** タブを開きます。
2. 取得した `Gemini APIキー` を入力し、保存します。
※ ここで設定したキーはシステム内部に暗号化保存され、クライアント側には一切表示されません。

### 3. 基本プロンプトの設定
AIに指示を出すための「共通のルール」を設定できます。
1. **[基本プロンプト設定]** タブを開きます。
2. 「この部屋に自然に家具を配置して」など、ベースとなるAIへの指示文を日本語で入力し保存します。
※ この文章は、クライアントが選んだスタイル名と結合されてAIに送信されます。

### 4. クライアント用ID・パスワードの発行方法
クライアント（顧客）がツールを利用するためのアカウントを発行します。
1. **[クライアント管理]** タブを開きます。
2. 「新規クライアント追加」の項目を展開します。
3. クライアントに渡す **「ログインID」** と **「パスワード」** を入力します。
4. そのクライアントが1ヶ月間に生成できる **「月間生成上限枚数」** を入力します（デフォルト: 50）。
5. 「追加」ボタンを押します。
6. **作成したID、パスワード、および本システムのアクセスURLをクライアントにお伝えください。**
※ パスワードは後から確認できないため、必ず控えてお伝えください。

### 5. クライアントの利用状況確認
1. **[利用状況・カウンター]** タブを開きます。
2. 登録されている全クライアントの「今月の生成枚数」と進捗バーを確認できます。

---

## 👤 クライアント（一般ユーザー）向けマニュアル

提供されたIDとパスワードでログインし、お部屋のバーチャルステージングを行う機能です。

### 1. ログイン
- 管理者から提供された **「ログインID」** と **「パスワード」** を入力してログインします。
- ログインすると、左側のメニューに「今月の生成枚数（利用状況）」が表示されます。

### 2. 写真の準備
入力方法は以下の2種類から選べます。
- **ファイルアップロード:** すでにスマホやPCに保存されているお部屋の写真（JPG/PNG）を選択してアップロードします。
- **カメラで撮影:** 「カメラで部屋を撮影してください」のボタンを押し、その場で部屋の写真を撮影します（スマホ等で利用時に便利です）。

### 3. 家具のスタイルを選択
配置したい家具のテイストを選択します。
- **モダン:** スタイリッシュで直線的なデザイン
- **北欧風:** 明るく、木目を活かした温かみのあるデザイン
- **インダストリアル:** レンガや金属を取り入れた無骨なデザイン
- **自由入力:** ご自身で好きな家具のイメージを自由に文章で入力できます（例：「赤いソファと観葉植物を置いて」など）。

### 4. 画像の生成
- 写真とスタイルを設定したら **「家具を配置する (画像を生成)」** ボタンを押します。
- 10秒〜20秒ほどでAIが処理を完了し、家具が配置された新しい写真が表示されます。
- ※ 画像生成が1回成功するごとに、左メニューの「今月の生成枚数」が1つカウントアップします。上限に達した場合は管理者に連絡してください。
